// Binary search is a method to find an element within an
// ordered list. To do this, we look for the middle element in the list
// and start looking for the target

// if its sorted, we can do better than O(n)

// Binary search is O(log n). The trees lookup method is a binary search
